# Persistence of Vision Raytracer 1.0

Copyright 1992 POV-Team

POV-Ray is based on DKBTrace 2.12 by David K. Buck and Aaron A. Collins.

## Contributing authors

- Steve A. Bennett: No removed contribution comments were identified in `src`.
- David K. Buck: Supplied the jittering fix used by the antialiasing code in `RenderEngine` and advised on the `HeightField` implementation.
- Aaron A. Collins: No removed contribution comments were identified in `src`.
- Alexander Enzmann: Contributed the `Box`, `Blob`, fourth- through sixth-order `PolynomialShape`, and bicubic `ParametricPatch` implementations; comments also associated him with related lexer tokens and the ray-origin offset in `MirrorReflectionShader`.
- Dan Farmer: Added the `RenderEngine` antialiasing jitter change based on David Buck's fix and the turbulence-octave support referenced by `PovrayMaterial`, `TextureParser`, and `Tokenizer`.
- Girish T. Hagan: No removed contribution comments were identified in `src`.
- Douglas Muir: Added the `HeightField` implementation.
- Bill Pulver: No removed contribution comments were identified in `src`.
- Robert Skinner: No removed contribution comments were identified in `src`.
- Scott Taylor: No removed contribution comments were identified in `src`.
- Drew Wells: Advised and supported the `HeightField` implementation.
- Chris Young: Added exponential-number parsing and lexer support for shadowless objects in `Tokenizer`.

The removed comments also contained the initials `CdW` and `RHA`, which cannot
be mapped reliably to names in the author list. `CdW` appeared in comments about
command-line defaults, image types, C-style comments, material maps, and verbose
rendering. `RHA` appeared in comments about brick textures and mortar.

Other contributors are listed in the documentation.
